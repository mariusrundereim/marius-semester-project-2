export function singleListing(media, seller, title, endsAt, bid, highestBid) {
  const listContainer = document.querySelector("#listing_container");
  listContainer.innerHTML = `<div>Hello ${title}</div>`;
}
